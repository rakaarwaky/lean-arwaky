# code-fizz fixture

A minimal solvable code task for the lean-ctx off-vs-on testbench.

`solution.sh` ships a deliberately wrong stub for `fizzbuzz()`. The model must
replace it with a correct implementation; `test.sh` is the oracle (exit 0 = pass).
