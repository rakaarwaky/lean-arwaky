# lean-ctx Rust Codebase

lean-ctx rules are in the root AGENTS.md — do not duplicate here.

## Subagents

Cursor blocks MCP in `readonly: true` subagents.
→ Launch with `readonly: false` + `subagent_type: "generalPurpose"` for ctx_* access.
