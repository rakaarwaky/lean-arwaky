[package]
name = "lean-ctx-protocol"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "Apache-2.0"
description = "Open wire contracts for lean-ctx: events, observations, assignments, evidence gaps."
repository = "https://github.com/yvgude/lean-ctx"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
