{
  "lane_id": "beets",
  "fixture_id": "medium-beetbox-beets",
  "sequence_id": "beets-lifecycle-sequence-v0",
  "repo_url": "https://github.com/beetbox/beets.git",
  "fixed_snapshot": "9acb1ecff6c7ee0a1e83e3b983c94792345712c5",
  "setup_deps": "uv sync 2>/dev/null || uv sync --frozen 2>/dev/null || true",
  "seed_patch_files": {
    "beets/util/functemplate.py": {
      "find": "                self.escapable_chars + extra_special_chars\n",
      "replace": "                self.escapable_chars\n"
    },
    "beets/dbcore/db.py": {
      "find": "        return iter(self._all_keys)\n",
      "replace": "        return (key for key in self._all_keys)\n"
    },
    "beetsplug/ftintitle.py": {
      "find": "    regex_explicit = re.compile(\n        plugins.feat_tokens(for_artist=False, custom_words=custom_words),",
      "replace": "    regex_explicit = re.compile(\n        plugins.feat_tokens(for_artist=for_artist, custom_words=custom_words),"
    }
  },
  "seed_add_files": {},
  "tasks": [
    {
      "id": "beets-lifecycle-feature-v0",
      "order": 1,
      "class": "feature-implementation",
      "allowed_files": ["beets/util/functemplate.py"],
      "verify_cmd": "uv run pytest -q test/util/test_functemplate.py -k 'escaped_sep' && uv run ruff format --check beets/util/functemplate.py",
      "prompt": "# Evaluation isolation contract\n\nThis is a treatment lane. Its profile was installed and configured before the task using every tool-author-recommended normal integration surface. The product's own installed guidance remains authoritative. The evaluator adds no invocation requirement, preference, quota, or forced call. Use the exposed environment naturally, and do not invoke a tool merely because this is a treatment lane. If no treatment tool is invoked after faithful installation, zero use is a valid observed outcome. Do not use unconfigured retrieval, compression, memory, or token-saving tools. Codex web search is disabled and model-launched shell commands have no network access; do not attempt curl, wget, browsers, package downloads, or any other external retrieval. Work only inside the target repository. The controller owns concealed verification; do not inspect or modify evaluation harness files.\n\n---\n\n\n# Sequential workflow session: beets-lifecycle-sequence-v0\n\nYou are in one persistent repository checkout. Do not reset the repository.\nYou will receive workflow prompts one at a time. Future prompts remain hidden until the current response completes.\nComplete only the current task. Preserve the working tree for later tasks.\nPreserve all previously repaired behavior and do not trade one disclosed task contract for another.\nSource, agent, tool, index, and cache state persist without controller resets between tasks.\nAll latent regressions are present in the initial repository, but only the current task prompt is disclosed.\nThe visible git baseline is the composite broken-start state; do not assume `git diff` or history reveals the intended fixes.\n\n## Current task 1: task-01\n\nRun only the exact command block in the current task prompt. It includes the complete focused acceptance for this task. Do not rerun earlier checks or inspect aggregate Git state; preserve earlier edits and stop when the block exits 0.\n\nBaseline V4 mechanical edit — restore escaped separators in function-template arguments. Do not discover or redesign anything.\n\n## Implementation recipe\n\nOnly `beets/util/functemplate.py` may change. Copy and run this command exactly:\n\n```bash\npython3 - <<'PY'\nfrom pathlib import Path\np = Path(\"beets/util/functemplate.py\")\ns = p.read_text()\nold = \"                self.escapable_chars\\n\"\nnew = \"                self.escapable_chars + extra_special_chars\\n\"\nassert s.count(old) == 1\np.write_text(s.replace(old, new))\nPY\nuv run --offline --frozen pytest -q test/util/test_functemplate.py -k 'escaped_sep'\nuv run --offline --frozen ruff format --check beets/util/functemplate.py\n```\n\n## Validation recipe\n\nThe command block above runs the complete focused validation.\n\n## Stop condition\n\nDo not inspect, search, modify tests, run anything else, or evaluate aggregate Git state. Run the command block exactly once and stop immediately when it exits 0. Earlier task edits intentionally remain in the persistent checkout."
    },
    {
      "id": "beets-lifecycle-refactor-v0",
      "order": 2,
      "class": "behavior-preserving-refactor",
      "allowed_files": ["beets/dbcore/db.py"],
      "verify_cmd": "uv run python -c \"from beets.dbcore.db import LazyDict; value=LazyDict({'a': 1}, lambda k,v:v); assert list(value)==['a']; assert type(iter(value)).__name__ == 'set_iterator'\" && uv run ruff format --check beets/dbcore/db.py",
      "prompt": "# Continue sequential workflow session: beets-lifecycle-sequence-v0\n\nContinue in the same source checkout and preserve earlier fixes.\nPreserve all previously repaired behavior and do not trade one disclosed task contract for another.\nThe controller did not run concealed verification, inject another regression, or refresh Git state between prompts.\nAll regressions remain present from the initial composite broken start; only later task prompts remain hidden.\nComplete only the current task. Repository, agent, tool, index, and cache state continue unchanged except for your own work.\n\n## Current task 2: task-02\n\nRun only the exact command block in the current task prompt. It includes the complete focused acceptance for this task. Do not rerun earlier checks or inspect aggregate Git state; preserve earlier edits and stop when the block exits 0.\n\nBaseline V4 mechanical refactor — remove one unnecessary generator layer. Do not discover or redesign anything.\n\n## Implementation recipe\n\nOnly `beets/dbcore/db.py` may change. Copy and run this command exactly:\n\n```bash\npython3 - <<'PY'\nfrom pathlib import Path\np = Path(\"beets/dbcore/db.py\")\ns = p.read_text()\nold = \"        return (key for key in self._all_keys)\\n\"\nnew = \"        return iter(self._all_keys)\\n\"\nassert s.count(old) == 1\np.write_text(s.replace(old, new))\nPY\nuv run --offline --frozen python -c \"from beets.dbcore.db import LazyDict; value=LazyDict({'a': 1}, lambda k,v:v); assert list(value)==['a']; assert type(iter(value)).__name__ == 'set_iterator'\"\nuv run --offline --frozen ruff format --check beets/dbcore/db.py\n```\n\n## Validation recipe\n\nThe command block above runs the complete focused validation.\n\n## Stop condition\n\nDo not inspect, search, modify tests, run anything else, or evaluate aggregate Git state. Run the command block exactly once and stop immediately when it exits 0. Earlier task edits intentionally remain in the persistent checkout."
    },
    {
      "id": "beets-lifecycle-review-v0",
      "order": 3,
      "class": "code-review-correction",
      "allowed_files": ["beetsplug/ftintitle.py"],
      "verify_cmd": "uv run pytest -q test/plugins/test_ftintitle.py -k 'split_on_feat' && uv run ruff format --check beetsplug/ftintitle.py",
      "prompt": "# Continue sequential workflow session: beets-lifecycle-sequence-v0\n\nContinue in the same source checkout and preserve earlier fixes.\nPreserve all previously repaired behavior and do not trade one disclosed task contract for another.\nThe controller did not run concealed verification, inject another regression, or refresh Git state between prompts.\nAll regressions remain present from the initial composite broken start; only later task prompts remain hidden.\nComplete only the current task. Repository, agent, tool, index, and cache state continue unchanged except for your own work.\n\n## Current task 3: task-03\n\nRun only the exact command block in the current task prompt. It includes the complete focused acceptance for this task. Do not rerun earlier checks or inspect aggregate Git state; preserve earlier edits and stop when the block exits 0.\n\nBaseline V4 mechanical review correction — restore the restricted explicit featuring-token set. Do not discover or redesign anything.\n\n## Implementation recipe\n\nOnly `beetsplug/ftintitle.py` may change. Copy and run this command exactly:\n\n```bash\npython3 - <<'PY'\nfrom pathlib import Path\np = Path(\"beetsplug/ftintitle.py\")\ns = p.read_text()\nold = \"        plugins.feat_tokens(for_artist=for_artist, custom_words=custom_words),\\n\"\nnew = \"        plugins.feat_tokens(for_artist=False, custom_words=custom_words),\\n\"\nassert s.count(old) == 1\np.write_text(s.replace(old, new))\nPY\nuv run --offline --frozen pytest -q test/plugins/test_ftintitle.py -k 'split_on_feat'\nuv run --offline --frozen ruff format --check beetsplug/ftintitle.py\n```\n\n## Validation recipe\n\nThe command block above runs the complete focused validation.\n\n## Stop condition\n\nDo not inspect, search, modify tests, run anything else, or evaluate aggregate Git state. Run the command block exactly once and stop immediately when it exits 0. Earlier task edits intentionally remain in the persistent checkout.\n\n\n## Proposed change under review\n\n```diff\ndiff --git a/beetsplug/ftintitle.py b/beetsplug/ftintitle.py\nindex 74d9d7ed..1c459628 100644\n--- a/beetsplug/ftintitle.py\n+++ b/beetsplug/ftintitle.py\n@@ -75,7 +75,7 @@ def split_on_feat(\n     # Try explicit featuring tokens first (ft, feat, featuring, etc.)\n     # to avoid splitting on generic separators like \"&\" when both are present\n     regex_explicit = re.compile(\n-        plugins.feat_tokens(for_artist=False, custom_words=custom_words),\n+        plugins.feat_tokens(for_artist=for_artist, custom_words=custom_words),\n         re.IGNORECASE,\n     )\n     parts = _split_on_feat_token(regex_explicit, artist)\n```"
    }
  ]
}
